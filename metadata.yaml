name: astrbot_plugin_gotify_push # 插件唯一识别名，以 astrbot_plugin_ 前缀开头
display_name: 监听 Gotify 消息并推送 # 用于展示的名字，可以是方便人类阅读的名字（需要版本 >= v4.5.0，低版本不会报错，请放心填写）
desc: 监听 Gotify 消息并推送 # 插件简短描述
version: v1.1.2 # 插件版本号。格式：v1.1.1 或者 v1.1
author: ksbjt # 作者
repo: https://github.com/ksbjt/astrbot_plugin_gotify_push # 插件的仓库地址
